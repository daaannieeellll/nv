return {
  component = require("nv.utils.status.component"),
  condition = require("nv.utils.status.condition"),
  env = require("nv.utils.status.env"),
  heirline = require("nv.utils.status.heirline"),
  hl = require("nv.utils.status.hl"),
  init = require("nv.utils.status.init"),
  provider = require("nv.utils.status.provider"),
  utils = require("nv.utils.status.utils"),
}
